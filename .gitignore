node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.DS_Store
/.idea/
*.iml

# The framed PNGs are committed — the README points at them. The bare captures
# are not: they are the same pixels without the frame, kept so that whoever
# redesigns the frame does not have to re-run the app to get them back.
/docs/media/raw/
